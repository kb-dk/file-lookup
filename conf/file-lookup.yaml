config:
  # The roots to track. It is advisable to use absolute paths
  roots:
    - '/tmp'
  # A scanInterval of -1 disables automatic scanning
  scanIntervalSeconds: -1
  # TODO: Implement these
  scanOnStartup: true
  filePattern: '.*'
